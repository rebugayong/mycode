#!/usr/bin/env python3
#hostname = 'MTG'
hostname = input('What is the of the host? ')

if hostname == 'Roel':
  print('The hostname was found to be Roel')

if hostname == 'mtg':
  print('The hostname was found to be ' + hostname)

if hostname == 'MTG':
  print('The hostname was found to be ' + hostname)

if hostname == 'mTg':
  print('The hostname was found to be ' + hostname)

if hostname == 'MTg':
  print('The hostname was found to be ' + hostname)

if hostname == 'mTG':
  print('The hostname was found to be ' + hostname)

if hostname == 'MtG':
  print('The hostname was found to be ' + hostname)

print("Exiting the script.")